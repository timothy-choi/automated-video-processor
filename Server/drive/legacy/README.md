These files were part of the original `Server/drive` Spring Boot source tree.

They are preserved for history, but they are **not compiled** by the Phase 1 control-service Maven build.

See `docs/legacy-system.md` for the classification and rationale.
